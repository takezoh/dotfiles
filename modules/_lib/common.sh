#!/bin/sh
# shellcheck shell=sh
# Shared helpers for module scripts — backward-compat shim for bootstrap.sh.
# MODULES_DIR must be set by the caller before sourcing this file.

. "$MODULES_DIR/_lib/bootstrap.sh"
