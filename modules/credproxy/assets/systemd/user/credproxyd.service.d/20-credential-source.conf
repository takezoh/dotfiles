[Service]
# The resolver accepts only systemd's service-private runtime directory and
# the fixed credential name.  No Environment= token or plaintext fallback.
UnsetEnvironment=OP_SERVICE_ACCOUNT_TOKEN
